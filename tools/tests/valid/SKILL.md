# Fixture skill

A fixture catalog the checker must pass clean.

## Index — the 2 principles

Reach for a principle when its cue describes the screen in front of you.

`PRINCIPLES-demo.md` — fixture family

- `UX-P90` Fixture principle — a fixture screen needs one entry that satisfies every rule
- `UX-P91` Fixture principle with a figure — a fixture screen states a quantity and must cite it

Each family file opens with its own ID→name index.
