# Fixture skill

A fixture catalog whose index has drifted from the `Cue.` lines it is generated
from.

## Index — the 9 principles

`PRINCIPLES-broken.md` — broken family

- `UX-P91` Bad vocabularies — a cue somebody edited here instead of in the entry

Each family file opens with its own ID→name index.
